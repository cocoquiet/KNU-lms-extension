{
  "name": "knu",
  "displayName": "KNU-lms-extension",
  "description": "경북대학교 학습관리시스템(LMS)의 기능을 사용할 수 있는 확장 프로그램입니다.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.89.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "LMS",
          "title": "KNU lms extension",
          "icon": "resources/knu_logo.svg"
        }
      ]
    },
    "views": {
      "LMS": [
        {
          "id": "course",
          "name": "course"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "course.refreshEntry",
          "when": "view == course",
          "group": "navigation"
        }
      ]
    },
    "commands": [
      {
        "command": "knu.checkapi",
        "title": "check api"
      },
      {
        "command": "course.refreshEntry",
        "title": "refresh",
        "icon": {
          "light": "resources/light/refresh.svg",
          "dark": "resources/dark/refresh.svg"
        
        }
      }
    ], 
    "configuration": {
      "title": "KNU-lms-extension",
      "properties": {
        "knu.token": {
          "type": "string",
          "default": "",
          "description": "LMS의 API를 사용하기 위한 토큰을 입력해주세요."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.89.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "18.x",
    "@typescript-eslint/eslint-plugin": "^7.7.1",
    "@typescript-eslint/parser": "^7.7.1",
    "eslint": "^8.57.0",
    "typescript": "^5.4.5",
    "@vscode/test-cli": "^0.0.9",
    "@vscode/test-electron": "^2.3.9"
  }
}
